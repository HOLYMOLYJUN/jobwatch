# 알림 (없으면 콘솔로 출력)
JW_SLACK_WEBHOOK_URL=

# 버튼(관심/패스)을 쓰려면 Socket Mode 용 토큰이 필요합니다
JW_SLACK_BOT_TOKEN=
JW_SLACK_APP_TOKEN=
JW_SLACK_CHANNEL=#jobs

# JW_DB_URL=            # 비워두면 var/jobwatch.db 를 씁니다
JW_REQUEST_DELAY_SEC=1.0
JW_USER_AGENT=jobwatch/0.1 (personal job alert bot)
